{
  "name": "test",
  "version": "1.0.0beta",
  "scripts": {
    "build": "parcel build src/pages/auth.page.hbs --no-cache --no-minify --public-url ./",
    "start": "parcel src/pages/auth.page.hbs --no-cache --port=3000",
    "serve": "serve -s dist"
  },
  "dependencies": {},
  "devDependencies": {
    "express": "^4.18.1",
    "parcel-bundler": "^1.12.5",
    "parcel-plugin-handlebars": "^0.4.4",
    "serve": "^13.0.2"
  }
}
